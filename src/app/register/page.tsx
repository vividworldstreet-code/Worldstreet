import Register from "@/components/register";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Create Account - Worldstreet Trading Platform",
};
const index = () => {
   return (
      <Wrapper>
         <Register />
      </Wrapper>
   )
}

export default index