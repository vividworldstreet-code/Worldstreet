import ResetPassWord from "@/components/reset-password";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Reset Password - Worldstreet Trading Platform",
};
const index = () => {
   return (
      <Wrapper>
         <ResetPassWord />
      </Wrapper>
   )
}

export default index